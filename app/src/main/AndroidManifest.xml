<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shivam.irsc_isafe">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.ChattingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.Signup" />
        <activity android:name=".Activities.Login" />
        <activity android:name=".Activities.OTPverify" />
        <activity android:name=".Activities.VerifyPhone" />
        <activity android:name=".Activities.Profile" />
        <activity android:name=".CreateActivity.Create_Activity" />
        <activity android:name=".CreateActivity.Events" />
        <activity android:name=".Notifications.Notifications"></activity>
    </application>

</manifest>